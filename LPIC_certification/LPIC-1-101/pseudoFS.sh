#!/bin/bash

# Contains information about the processes running on a system.
# Processes are listed by PID, with hardware and process data
# both in the same directory structure
ls /proc

# Contains information about the
# system’s hardware and kernel modules.
ls /sys





